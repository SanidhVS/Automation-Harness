/** Single source of truth for the product's identity. Renaming the product requires
 * changing only this file and `package.json` (`name`, `bin`). */
export const PRODUCT_NAME = 'rerun';
export const PRODUCT_DISPLAY_NAME = 'Rerun';
